# CLAUDE.md

Claude Code reads `CLAUDE.md`; other agents (Codex and most of the rest) read
`AGENTS.md`. Rather than maintaining two copies that drift apart, this file
imports the real one.

@AGENTS.md

Do not add rules below this line. Everything belongs in `AGENTS.md`, so that
every agent working in this repository is held to the same standard. The only
thing that should ever live here is genuinely Claude-Code-specific
configuration that no other tool understands.
